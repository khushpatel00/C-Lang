#include <stdio.h>
#include <conio.h>

main(){
	int a1,a2,ans;
        
	clrscr();
		printf("Enter the value of first angle of triangle : ");
		scanf("%d",&a1);
		printf("Enter the value of second angle of triangle : ");
		scanf("%d",&a2);

		//logic
		ans = 180 - (a1+a2);
		if(ans>180){
		printf("invalid ans..");
		}
		else{
			printf("the value of third angle is %d",ans);
		}


	getch();
}