#include <stdio.h>
#include <conio.h>
main(){
    int math, eng, sc, ans;
    clrscr();
    start:
    printf("enter the marks of Maths (out of 100) : ");
    scanf("%d",&math);
	if(math > 100)
	{
		printf("enter valid marks again : ");
	scanf("%d",&math);
	}

    printf("enter the marks of English (out of 100) : ");
    scanf("%d",&eng);
    if(eng > 100)
	{
		printf("enter valid marks again : ");
	scanf("%d",&eng);
	}
    printf("enter tha marks of Science (out of 100) : ");
    scanf("%d",&sc);
    if(sc > 100)
	{
		printf("enter valid marks again : ");
	scanf("%d",&sc);
	}

    ans = (math + eng + sc) / 3;
	if(ans > 100)
	{
	    printf("there was an error getting the average marks,\nplease try again ");
	    goto start;

	}

    printf("the average marks are %d",ans);

    getch();
}