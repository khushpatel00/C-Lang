#include <stdio.h>
#include <conio.h>

main()
{
    clrscr();
    int a, b, ans;
    printf("Enter the first angle: ");
    scanf("%d", a);
    printf("Enter the second angle: ");
    scanf("%d", b);
    ans = 180 - a - b;
    printf("The third angle is %d", ans);

    getch();
}