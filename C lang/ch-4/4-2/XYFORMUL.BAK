#include<stdio.h>
#include<conio.h>

main() {

	long int x,y,ans;
	char ch;

	clrscr();
	printf("enter a number to apply in an formula : ");
	scanf("%d",x);
	printf("\nEnter second number : ");
	scanf("%d",y);
	ans = (x*x) - 2*x*y + (y*y);
	printf("the value after applying the formula is : %d",ans);
	getch();
}
